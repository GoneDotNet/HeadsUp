<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:headsUp="clr-namespace:GoneDotNet.HeadsUp"
             x:Class="GoneDotNet.HeadsUp.TestPage"
             x:DataType="headsUp:TestViewModel"
             Title="Test Page">
    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding Events}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="headsUp:EventItem">
                    <Grid ColumnDefinitions="1*, 1*">
                        <Label Text="{Binding Text}" 
                               Grid.Column="0"/>
                        
                        <Label Text="{Binding Timestamp, StringFormat='{0:HH:mm:ss}'}"
                               Grid.Column="1"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>